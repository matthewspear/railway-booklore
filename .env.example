# Application Settings
USER_ID=1000
GROUP_ID=1000
TZ=Etc/UTC

# Database Connection (auto-configured by Railway template)
DATABASE_URL=jdbc:mariadb://mariadb:3306/booklore
DATABASE_USERNAME=booklore
DATABASE_PASSWORD=your-secure-password-here

# Storage Configuration
DISK_TYPE=LOCAL

# MariaDB Settings (auto-configured by Railway template)
DB_USER_ID=1000
DB_GROUP_ID=1000
MYSQL_ROOT_PASSWORD=your-root-password-here
MYSQL_DATABASE=booklore
MYSQL_USER=booklore
MYSQL_PASSWORD=your-secure-password-here

# Optional: OIDC Authentication
# OIDC_ENABLED=true
# OIDC_PROVIDER_URL=https://your-provider.com
# OIDC_CLIENT_ID=your-client-id
# OIDC_CLIENT_SECRET=your-client-secret

# Optional: Email/SMTP (for Kindle delivery)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-password
# SMTP_FROM=booklore@example.com
